name: 🎨 AstraVault AI – Code Style Enforcement

on:
  pull_request:
    branches: [ "main" ]

jobs:
  style:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: psf/black@stable
      - run: |
          pip install flake8
          flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
